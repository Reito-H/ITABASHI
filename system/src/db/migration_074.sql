-- ===================================================
-- migration_074: 班長シフトのセルに「確定（ロック）」を追加
--   確定済みのセルは色付き枠で表示し、解除するまで内容の編集をブロックする。
-- ===================================================

ALTER TABLE kancho_shifts ADD COLUMN is_locked INTEGER NOT NULL DEFAULT 0;
