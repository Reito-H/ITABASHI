-- ===================================================
-- migration_036: 班長シフト 非番も出勤数にカウント
--   当直明けの「非」は1日出勤扱い（カウント列としての個別表示はしない）
-- ===================================================

UPDATE kancho_shift_types SET counts_as_work = 1 WHERE code = '非';
