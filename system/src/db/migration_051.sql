-- ===================================================
-- migration_051: 班長シフト 前任者リンク
--   月度ごとに独立化した名簿(kancho_members)に「前任者(先月度の誰か)」への
--   自己参照を追加。月をまたぐグレー表示日の自動反映と、名前欄の
--   「旧名→新名」表示に使う。
-- ===================================================

ALTER TABLE kancho_members ADD COLUMN prev_member_id INTEGER;
